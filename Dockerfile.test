ARG BASE_IMAGE=nvidia/cuda:12.6.3-cudnn-devel-ubuntu22.04
FROM ${BASE_IMAGE}

WORKDIR /app

# Base system dependencies
ARG CACHE_BUST_LAYER_1=1
RUN echo "Layer 1 cache bust: ${CACHE_BUST_LAYER_1}" && \
    apt-get update && apt-get install -y \
    build-essential \
    cmake \
    git \
    curl \
    wget \
    vim \
    software-properties-common \
    ca-certificates \
    gnupg \
    lsb-release \
    && rm -rf /var/lib/apt/lists/*

# Node.js and npm packages
ARG CACHE_BUST_LAYER_2=1
RUN echo "Layer 2 cache bust: ${CACHE_BUST_LAYER_2}" && \
    curl -fsSL https://deb.nodesource.com/setup_20.x | bash - \
    && apt-get install -y nodejs \
    && rm -rf /var/lib/apt/lists/* \
    && npm install -g \
        typescript \
        webpack \
        webpack-cli \
        @angular/cli \
        create-react-app \
        next \
        prettier \
        eslint

# Python
ARG CACHE_BUST_LAYER_3=1
RUN echo "Layer 3 cache bust: ${CACHE_BUST_LAYER_3}" && \
    apt-get update && apt-get install -y \
        python3.11 \
        python3-pip \
        python3-dev \
    && rm -rf /var/lib/apt/lists/*

# PyTorch
ARG CACHE_BUST_LAYER_4=1
RUN echo "Layer 4 cache bust: ${CACHE_BUST_LAYER_4}" && \
    pip3 install --no-cache-dir \
        torch \
        torchvision \
        torchaudio

# TensorFlow
ARG CACHE_BUST_LAYER_5=1
RUN echo "Layer 5 cache bust: ${CACHE_BUST_LAYER_5}" && \
    pip3 install --no-cache-dir \
        tensorflow \
        keras

# ML/Data Science libraries
ARG CACHE_BUST_LAYER_6=1
RUN echo "Layer 6 cache bust: ${CACHE_BUST_LAYER_6}" && \
    pip3 install --no-cache-dir \
        numpy \
        scipy \
        pandas \
        scikit-learn \
        matplotlib \
        seaborn \
        plotly \
        jupyter \
        jupyterlab \
        notebook

# NLP and Transformers
ARG CACHE_BUST_LAYER_7=1
RUN echo "Layer 7 cache bust: ${CACHE_BUST_LAYER_7}" && \
    pip3 install --no-cache-dir \
        transformers \
        datasets \
        tokenizers \
        sentencepiece \
        nltk \
        spacy

# Computer Vision libraries
ARG CACHE_BUST_LAYER_8=1
RUN echo "Layer 8 cache bust: ${CACHE_BUST_LAYER_8}" && \
    pip3 install --no-cache-dir \
        opencv-python \
        opencv-contrib-python \
        pillow \
        imageio \
        scikit-image

# Go
ARG CACHE_BUST_LAYER_9=1
RUN echo "Layer 9 cache bust: ${CACHE_BUST_LAYER_9}" && \
    wget https://go.dev/dl/go1.22.0.linux-amd64.tar.gz \
    && tar -C /usr/local -xzf go1.22.0.linux-amd64.tar.gz \
    && rm go1.22.0.linux-amd64.tar.gz
ENV PATH="${PATH}:/usr/local/go/bin"

# Rust
ARG CACHE_BUST_LAYER_10=1
RUN echo "Layer 10 cache bust: ${CACHE_BUST_LAYER_10}" && \
    curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
ENV PATH="/root/.cargo/bin:${PATH}"

# Java
ARG CACHE_BUST_LAYER_11=1
RUN echo "Layer 11 cache bust: ${CACHE_BUST_LAYER_11}" && \
    apt-get update && apt-get install -y \
        openjdk-17-jdk \
        maven \
        gradle \
    && rm -rf /var/lib/apt/lists/*

# Database clients
ARG CACHE_BUST_LAYER_12=1
RUN echo "Layer 12 cache bust: ${CACHE_BUST_LAYER_12}" && \
    apt-get update && apt-get install -y \
        postgresql-client \
        mysql-client \
        redis-tools \
    && rm -rf /var/lib/apt/lists/*

# Deep Learning frameworks
ARG CACHE_BUST_LAYER_13=1
RUN echo "Layer 13 cache bust: ${CACHE_BUST_LAYER_13}" && \
    pip3 install --no-cache-dir \
        jax \
        jaxlib \
        optax \
        flax

# ML tools
ARG CACHE_BUST_LAYER_14=1
RUN echo "Layer 14 cache bust: ${CACHE_BUST_LAYER_14}" && \
    pip3 install --no-cache-dir \
        xgboost \
        lightgbm \
        catboost \
        statsmodels \
        prophet

# Docker CLI
ARG CACHE_BUST_LAYER_15=1
RUN echo "Layer 15 cache bust: ${CACHE_BUST_LAYER_15}" && \
    curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg \
    && echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | tee /etc/apt/sources.list.d/docker.list > /dev/null \
    && apt-get update \
    && apt-get install -y docker-ce-cli \
    && rm -rf /var/lib/apt/lists/*

# Kubernetes tools
ARG CACHE_BUST_LAYER_16=1
RUN echo "Layer 16 cache bust: ${CACHE_BUST_LAYER_16}" && \
    curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl" \
    && install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl \
    && rm kubectl

# Terraform
ARG CACHE_BUST_LAYER_17=1
RUN echo "Layer 17 cache bust: ${CACHE_BUST_LAYER_17}" && \
    wget -O- https://apt.releases.hashicorp.com/gpg | gpg --dearmor -o /usr/share/keyrings/hashicorp-archive-keyring.gpg \
    && echo "deb [signed-by=/usr/share/keyrings/hashicorp-archive-keyring.gpg] https://apt.releases.hashicorp.com $(lsb_release -cs) main" | tee /etc/apt/sources.list.d/hashicorp.list \
    && apt-get update && apt-get install -y terraform \
    && rm -rf /var/lib/apt/lists/*

# Web scraping
ARG CACHE_BUST_LAYER_18=1
RUN echo "Layer 18 cache bust: ${CACHE_BUST_LAYER_18}" && \
    pip3 install --no-cache-dir \
        selenium \
        beautifulsoup4 \
        scrapy \
        playwright \
    && playwright install --with-deps chromium

# Web frameworks
ARG CACHE_BUST_LAYER_19=1
RUN echo "Layer 19 cache bust: ${CACHE_BUST_LAYER_19}" && \
    pip3 install --no-cache-dir \
        fastapi \
        uvicorn \
        django \
        flask \
        requests \
        httpx \
        aiohttp

# Data processing
ARG CACHE_BUST_LAYER_20=1
RUN echo "Layer 20 cache bust: ${CACHE_BUST_LAYER_20}" && \
    pip3 install --no-cache-dir \
        sqlalchemy \
        psycopg2-binary \
        pymongo \
        redis \
        celery

CMD ["python3", "--version"]